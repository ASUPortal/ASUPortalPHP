<div class="control-group">
    {CHtml::activeLabel("person[add_work]", $form)}
    <div class="controls">
    {CHtml::activeTextField("person[add_work]", $form)}
    {CHtml::error("person[add_work]", $form)}
</div></div>

<div class="control-group">
    {CHtml::activeLabel("person[tel_work]", $form)}
    <div class="controls">
    {CHtml::activeTextField("person[tel_work]", $form)}
    {CHtml::error("person[tel_work]", $form)}
</div></div>

<div class="control-group">
    {CHtml::activeLabel("person[add_home]", $form)}
    <div class="controls">
    {CHtml::activeTextField("person[add_home]", $form)}
    {CHtml::error("person[add_home]", $form)}
</div></div>

<div class="control-group">
    {CHtml::activeLabel("person[tel_home]", $form)}
    <div class="controls">
    {CHtml::activeTextField("person[tel_home]", $form)}
    {CHtml::error("person[tel_home]", $form)}
</div></div>

<div class="control-group">
    {CHtml::activeLabel("person[e_mail]", $form)}
    <div class="controls">
    {CHtml::activeTextField("person[e_mail]", $form)}
    {CHtml::error("person[e_mail]", $form)}
</div></div>

<div class="control-group">
    {CHtml::activeLabel("person[add_contact]", $form)}
    <div class="controls">
    {CHtml::activeTextBox("person[add_contact]", $form)}
    {CHtml::error("person[add_contact]", $form)}
</div></div>

<div class="control-group">
    {CHtml::activeLabel("person[site]", $form)}
    <div class="controls">
    {CHtml::activeTextField("person[site]", $form)}
    {CHtml::error("person[site]", $form)}
</div></div>